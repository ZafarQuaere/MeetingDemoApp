<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/home"
        android:icon="@drawable/ic_home_tab"
        android:contentDescription="@string/home_tab_home"
        android:title="@string/home_tab_home"
        android:visible="false"/>

    <item
        android:id="@+id/agenda"
        android:icon="@drawable/ic_calendar"
        android:contentDescription="@string/home_tab_meetings"
        android:title="@string/home_tab_meetings"
        android:visible="false"/>
</menu>